#include<iostream>
using namespace std;

int main()
{
    int a, b, c;
    
    cin >> a >> b;

    // sum of two numbers in stored in variable sumOfTwoNumbers(c)
    c = a + b;

    // Prints sum 
    cout << c <<"\n";     

    return 0;
}